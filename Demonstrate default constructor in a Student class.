#include<iostream>
using namespace std;
class Student {
    int ID;
    string name;
public:
    Student() : ID(0), name("Unknown") {}
    void display() {
        cout << "ID: " << ID << ", Name: " << name << endl;
    }
};
int main() {
    Student s;
    s.display();
    return 0;
}
